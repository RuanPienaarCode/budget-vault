{
  "name": "budget-vault",
  "version": "1.26.4",
  "description": "Track your budget, spending, debt and savings as plain files in your own vault.",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "node scripts/run-tests.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10"
  },
  "engines": {
    "node": ">=18"
  }
}
